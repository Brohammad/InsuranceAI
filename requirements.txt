# ─────────────────────────────────────────────
#  Project RenewAI  —  Python Dependencies
# ─────────────────────────────────────────────

# Google Gemini
google-generativeai>=0.8.0
google-genai>=1.0.0

# LangChain + LangGraph (orchestration)
langchain>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0

# Vector store (local)
chromadb>=0.5.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database
# (sqlite3 is stdlib — no install needed)

# HTTP / API
httpx>=0.27.0
requests>=2.32.0

# Email (local SMTP / MailHog)
# (smtplib is stdlib — no install needed)

# WhatsApp (Twilio)
twilio>=9.3.0

# Voice (ElevenLabs / Sarvam — cloud-safe)
# pyttsx3 and openai-whisper require system deps (ffmpeg/espeak) —
# omitted here; voice delivery uses ElevenLabs REST API instead.
elevenlabs>=1.0.0

# Scheduling & async
apscheduler>=3.10.0

# Dashboard
streamlit>=1.36.0
plotly>=5.22.0
pandas>=2.2.0

# Utilities
python-dotenv>=1.0.0
rich>=13.7.0
loguru>=0.7.2
jinja2>=3.1.4

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

